# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
# Map static resources from the /public folder to the /assets URL path
GET /assets/*file controllers.Assets.at(path="/public", file)

################# User Controller###############

GET /parse controllers.ForumController.parseData()

GET /persist controllers.ForumController.persist()

GET /find controllers.ForumController.find()

GET /threads_count controllers.ForumController.threadsFrom2013()